export class Product {
    id: number;
    image: string;
    available: boolean;
    name: string;
    priceUnit: number;
    description: string;

    constructor(name: string, price: number, description: string){

    }
}